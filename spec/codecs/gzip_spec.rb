# encoding: utf-8

require "logstash/plugin"

describe "LogStash::Codecs::Gzip" do
  it "should compress message" do
    
  end
  
  it "should uncompress message" do
    
  end

  #If there are multiple lines that are gzipped then they should be sent as 
  #individual evnts
  it "should handle multiple compressed messages" do
    
  end
end
